#include <bits/stdc++.h>
using namespace std;
#define int long long 
#define pb push_back

int32_t main(){
    int t;
    cin>>t;
    while(t--){
        int n,f;
        cin>>n>>f;
        f++;
        int a[n];
        sort(a,a+n);
        int lo=0,hi=a[n-1],mid;
        
        while (lo<=hi)
        {
            mid=(hi+lp)/2;

        }
                
    }

    return 0;
}